[pytest]
# Tests live under backend/tests and import the app as `app.*` / `ml.*`,
# so the backend directory itself must be on sys.path (rootdir == backend).
testpaths = tests
python_files = test_*.py
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
